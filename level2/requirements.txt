# A股 Level 2 数据实时接收处理系统 - 依赖包

# 核心依赖
msgpack>=1.0.0          # 高性能序列化库（比JSON快5-10倍）
numpy>=1.20.0           # 数值计算库

# XTDATA依赖（需要单独安装QMT客户端）
# xtdata  # 从QMT安装包中获取，无法通过pip安装

# 测试依赖
pandas>=1.3.0           # 数据分析（测试脚本需要）
pyarrow>=6.0.0          # Feather文件格式支持
tqdm>=4.60.0            # 进度条显示

# 可选依赖
# redis>=4.0.0          # 分布式缓存（用于结果共享）
playwright>=1.40.0     # 浏览器自动化（问财导出下载）