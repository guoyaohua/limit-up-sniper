# 参与贡献

感谢对 Limit-Up Sniper 的关注。这里同时包含交易执行与研究代码；所有变更都应以
安全、可复现和可审计为前提。

## 提交前

1. 先搜索现有 Issue，避免重复讨论。安全漏洞请按 [SECURITY.md](SECURITY.md)
   私下报告，不要创建公开 Issue。
2. 从最新的 `main` 创建短生命周期分支。一个 Pull Request 只解决一个明确问题。
3. 不要提交账号、密钥、Cookie、券商路径、原始交易日志或未经授权的行情数据。
4. 新功能默认保持 `simulation` 安全模式；不得绕过实盘启动时的显式确认。

## 本地验证

```powershell
python -m pip install -r requirements-dev.txt
python scripts/scan_secrets.py
python -m pytest -q
```

需要 QMT、网络、浏览器、凭据或本地行情的集成测试不会进入默认离线套件。若变更
涉及这些功能，请在 Pull Request 中记录额外的验证命令、环境和脱敏结果。

## 策略变更的证据要求

策略优化不能只报告收益变高。Issue 和 Pull Request 至少应包含：

- 假设：为什么该信号在交易机制上可能有效；
- 基线：对比的提交、策略版本和参数；
- 数据：日期范围、股票池、频率、数据来源及缺失值处理；
- 成交模型：涨跌停不可成交、排队、滑点、手续费、印花税与最小交易单位；
- 指标：信号数、成交数、封板率、次日收益、累计/年化收益、最大回撤、胜率和盈亏比；
- 验证：训练/验证/样本外区间，最好同时提供滚动或 walk-forward 结果；
- 偏差检查：未来函数、幸存者偏差、重复 Tick、停牌和退市样本；
- 风险：弱市、低流动性、行情中断、重启恢复及参数失效时的表现。

封板率需声明口径。仓库推荐使用：

$$
\text{封板率}=\frac{\text{买入后收盘仍处于涨停的成交标的数}}{\text{实际成交标的数}}
$$

同时报告 Wilson 置信区间或样本量，避免把小样本波动当作稳定提升。调参区间与最终
样本外区间必须隔离；任何用于当日决策的数据，其时间戳不得晚于决策时间。

## 代码与数据约定

- 遵循现有 Python 风格，函数和变量保持语义明确；公共接口变更应同步文档。
- 修复缺陷时添加回归测试；新增策略条件时覆盖通过、拒绝和边界场景。
- 时间统一明确交易所时区，金额与价格计算避免隐式精度转换。
- 生成数据放入已忽略的 `output/`、`reports/` 或配置的数据目录，不要提交大文件。
- 第三方行情、网页和 SDK 的使用须遵守其许可、服务条款和数据再分发限制。

## Pull Request

PR 描述应说明动机、影响范围、验证结果和回滚方式。策略结果优先附结构化摘要或
可复现命令，不要上传包含个人信息、凭据或受限行情的原始附件。维护者可能要求先在
模拟 A/B 或逐 Tick 回放中观察，再决定是否进入默认策略。
